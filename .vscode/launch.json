{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Webserver",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd",
            "cwd": "${workspaceFolder}",
            "env": {},
            "args": [
                "patcher"
            ]
        }
    ]
}